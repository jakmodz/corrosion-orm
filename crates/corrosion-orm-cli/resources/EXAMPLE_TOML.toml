[package]
name = "migrations"
edition = "2024"
version = "0.1.0"

[dependencies]
anyhow = "1"
async-trait = "0.1"
tokio = { version = "1", features = ["full"] }
corrosion-orm = { path = "", features = ["sqlite"] }
corrosion-orm-migration = { path = "" }

[workspace]
